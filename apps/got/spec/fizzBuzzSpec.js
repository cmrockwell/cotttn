/**
 * @author Chris
 */
describe('FizzBuzz', function () {
    it('if a number is divisble by 3 print Fizz', function () {
        var fb = new fizz_buzz();
        var bool = fb;
        expect(actual).toBe(4);
    });

});
